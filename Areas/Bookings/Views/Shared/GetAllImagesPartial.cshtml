@model IEnumerable<HotelApplication.Models.Image>

<div class="row">
    @foreach (var item in Model)
    {
        <div class="col-md-3 panel">
            <div style="background-color: #f7f7f5; border-radius:5px; padding: 2px;" class="imageItem" id="@item.Id">
                <div>
                    <input type="checkbox" name="selectedImages" value="@item.Id" data-url="@item.ImageUrl" style="float:left;" />
                    <img class="img-thumbnail img-responsive" src="@item.ImageUrl" asp-append-version="true" alt="@item.Name" width="200" />
                    <div class="panel-group" style="padding:5px;">
                        <p>@item.Name</p>
                    </div>
                </div>
            </div>
        </div>
    }
</div>